(function () {
	let lokation = document.querySelector(".lokation");
	let card = document.querySelector(".card");
	//let MaxWidthLokation = card - lokation;

	if (card + lokation > region) {}
	calendar.insertAdjacentHTML("beforeend", `
        <p>...</p>
    `);
}());
